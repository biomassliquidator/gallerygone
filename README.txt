Windows 11 Gallery Removal is a simple script that lets you disable or enable
the Gallery section of the Windows 11 File Explorer.

Make sure to run the script as Administrator and select whichever option you want.
All the script does is either deletes or adds the registry key for Gallery.